services:
  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postresql/data
    environment:
      - POSTGRES_PASSWORD=postgrespw
volumes:
  pgdata:
